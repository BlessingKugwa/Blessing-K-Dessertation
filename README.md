# Operational Risk Management System

## 🚀 Quick Start (Double-click run_dashboard.bat)

1. **Install:** `pip install -r requirements.txt`
2. **Run:** `run_dashboard.bat`
3. **Open:** http://localhost:8501

## Files
- `dashboard.py` - Streamlit app
- `op_risk_model.py` - Train models
- `outputs/` - Models + data
- `Operational risk losses data.csv` - Sample data

## Test
Tab2 → Upload sample CSV → RUN → See predictions!

**Production Ready!**
